#!/bin/sh

. ~/.yawm/colors/menu-colors

ctl_=$HOME/.yawm/.control$DISPLAY
cli_=$HOME/.yawm/tmp/clients

echo list-clients > $ctl_

menu -n yawm-clients -b $colors_ -s 5 -d $cli_ |
while read scr tag win info; do
	echo "focus-window $win" > $ctl_
	exit 0
done
